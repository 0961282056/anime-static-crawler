name: Scheduled Anime Crawler

on:
  schedule:
    # 每天 UTC 17:00 執行 (台灣時間 01:00)
    - cron: '0 17 * * *'
  workflow_dispatch:

jobs:
  crawl-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      # 【優化 1】開啟 pip 快取，大幅加快安裝速度
      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # 自動快取 pip 依賴

      # 這裡會優先從快取還原，若 requirements.txt 沒變，幾秒鐘就跑完
      - name: ⚙️ Install dependencies
        run: |
          pip install -r requirements.txt

      # 【優化 2】明確指定 BUILD_ONLY 為 false，確保執行爬蟲
      - name: 🕷️ Run Crawler
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          BUILD_ONLY: false 
        run: |
          python generate_static.py

      - name: 💾 Commit and Push new data
        run: |
          git config --global user.name 'GitHub Action Crawler'
          git config --global user.email 'action@github.com'
          
          # 加入 dist 資料夾與快取檔
          git add dist/data/*.json
          git add cloudinary_cache.json
          
          # 檢查是否有變更，有變更才 Commit
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "🤖 Auto-update anime data & cache"
            git push
            echo "✅ Data pushed to GitHub. Cloudflare Pages should trigger build now."
          fi
